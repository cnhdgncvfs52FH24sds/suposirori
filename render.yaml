services:
  - type: web
    name: previewhub
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: |
      gunicorn server.app:app --bind 0.0.0.0:$PORT --workers 2
    autoDeploy: true
